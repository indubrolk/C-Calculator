#include <stdio.h>

int main(void)
    {
  char operator;
  double operand1, operand2;

  printf("Enter operator (+, -, *, /, ): ");
  scanf("%c", &operator);

  printf("Enter operands (numbers): ");

  return 0;

    }